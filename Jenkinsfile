pipeline {
    agent any
    environment {
        IMAGE_NAME = "devopssteps/my-app"
    }

    stages {
        stage('GitHub') {
            steps {
                echo "${IMAGE_NAME)
            }
        }
    }
}
